#!/usr/bin/zsh
PATH='/usr/bin'
HOME='/'
if [[ "x$1" == "xadd" ]; then
    chezmoi -S "/home/$(id -un)/.local/share/chezmoi/root" $@
else
    sudo -E chezmoi -S "/home/$(id -un)/.local/share/chezmoi/root" $@
    sudo -k
fi
